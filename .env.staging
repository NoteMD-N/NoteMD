# Staging — synthetic data only. Never load real patient information here.
#
# These values are public: the URL is a public API endpoint and the publishable
# (anon) key is embedded in every frontend bundle. Access is controlled by Row
# Level Security, not by keeping this key secret. The service-role key and the
# database password are NOT public and must never appear in this repository.
VITE_APP_ENV=staging
VITE_SUPABASE_PROJECT_ID=toeurvqqucloareeujfa
VITE_SUPABASE_URL=https://toeurvqqucloareeujfa.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_yxzsIcSLGifxxPXkSDOzDA_HT3e1mwY
